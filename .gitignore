# Signing keys — never commit
keys/

# Connect IQ build intermediates (regenerated by monkeyc)
gen/
internal-mir/
external-mir/
bin/

# Build outputs
*.prg.debug.xml
SailVMG_test.prg
*.iq

# Scratch
trace_resume.txt
