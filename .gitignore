# Signing keys — never commit
keys/

# Connect IQ build intermediates (regenerated by monkeyc)
gen/
internal-mir/
external-mir/
bin/

# Build outputs
*.prg.debug.xml
SailVMG_test.prg
*.iq

# Local-only simulator fake-data build (enables SimConfig via :notsimdata).
# Absent by default -> every committed/release build has fake data OFF.
monkey.sim.jungle

# Scratch
trace_resume.txt
